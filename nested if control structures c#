// this code used nested if to see if an employee is to be promoted based
// on number of years and perfomance, if he has served the required number 
// of years and his perfomance is good he/she is promoted else he/she is not promoted

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        int yearsserved, perfomancescore;
        
    
        Console.WriteLine ("enter years searved ");
        yearsserved=Convert.ToInt32(Console.ReadLine());
        
        Console.WriteLine ("enter perfomance (in percentage)");
        perfomancescore=Convert.ToInt32(Console.ReadLine());
        
        if (yearsserved>=5)
        {
            if (perfomancescore>= 70 )
            {
              Console.WriteLine ("you are eligible for promotion ");}
        }
//in nested if control structures a second if statement is inside the first one. 
        else
         ("you are not eligible for prootion");}
      }
    }
}
