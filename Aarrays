// Online C# Editor for free
// Write, Edit and Run your C# code using C# Online Compiler

using System;

public class HelloWorld
{
    public static void Main(string[] args)
    {
        
        int[] numbers= new int [5]; // array initialization
        numbers = new int[5]{10,9,8,7,6}; // array declaration
        numbers[1]=17;
        
            // displaying the values stored in an array
         Console.WriteLine (" the first value "+ numbers[0]);
         Console.WriteLine (" the third value "+ numbers[1]);
         Console.WriteLine (" the fouth value "+ numbers[2]);
         Console.WriteLine (" the fifth value "+ numbers[3]);
         Console.WriteLine (" the sixth value "+ numbers[4]);
         Console.WriteLine();
         
             // changing values in an array
        Console.WriteLine(" ==== changing values =====");
         
        numbers[0]=17;
        numbers[1]=23;
        numbers[2]=46;
        numbers[3]=89;
        numbers[4]=114;
        
        Console.WriteLine( "the new first number is " + numbers[0]);
        Console.WriteLine( "the new second number is " + numbers[1]);
        Console.WriteLine( "the new third number is " + numbers[2]);
        Console.WriteLine( "the new four number is " + numbers[3]);
        Console.WriteLine( "the new fifth number is " + numbers[4]);
        Console.WriteLine();
        
       Console.WriteLine(" ====using loop to print all values=====");
        
        int count=0;
        while (count <5) {
            Console.WriteLine(numbers [count]);
            count ++;
        }
            
        
        
        
        
        
         
         
    }
}
